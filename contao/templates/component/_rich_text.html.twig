{% use '@Contao/component/_rich_text.html.twig' %}

{% block rich_text_attributes %}
    {% set rich_text_attributes = attrs(rich_text_attributes|default)
        .addClass(styles.size(data.textAppearance|default))
    %}
    {{ parent() }}
{% endblock %}
