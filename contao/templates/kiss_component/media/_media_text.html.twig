{% use '@Contao/kiss_component/media/_text.html.twig' %}

{% block media_text %}
    {% set item = item|default(_context) %}

    {% block media %}
        {% if type|default in ['image', 'icon'] %}
            <div{{ attrs().addClass('media').mergeWith(media_attributes|default) }}>
                {{ include("@Contao/kiss_component/media/_#{type}.html.twig") }}
            </div>
        {% endif %}
    {% endblock %}

    {% if item.text|default or headline.text %}
        <div{{ attrs().addClass('text').mergeWith(text_attributes|default) }}>
            {% block headline %}{% endblock %}
            {% with {text: item.text|default} %}
                {{ block('text') }}
            {% endwith %}
        </div>
    {% endif %}
{% endblock %}
