{% block icon %}
    {% set item = item|default(_context) %}

    {% if item.icon|default %}
        <div{{ media_attributes|default }}>
            {{ svg_icon(item.icon) }}
        </div>
    {% endif %}
{% endblock %}
