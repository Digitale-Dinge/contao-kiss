{% set prefix = prefix ?? 'text-' %}
{% set span_attributes = attrs(span_attributes|default)
    .addClass(prefix ~ value, value)
%}
<span{{ span_attributes }}>{{- content|sanitize_html('contao')|insert_tag_raw -}}</span>
