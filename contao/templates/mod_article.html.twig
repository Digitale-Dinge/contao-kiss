{% extends '@Contao/mod_article' %}

{% set wrapperAttributes = attrs(cssID)
    .addClass([
        styles.container(contentWidth|default),
        styles.background(bgColor|default),
        styles.padding_top(paddingTop|default),
        styles.padding_bottom(paddingBottom|default),
    ])
    .mergeWith(wrapperAttributes|default)
%}
