<div{{ attrs(header_meta_attributes|default).addClass(['header-meta', 'relative', 'py-1', 'bg-header-meta']) }}>
    <div{{ attrs(header_meta_inside_attribute|default).addClass(['inside', 'flex', 'gap-x-4', 'items-center', 'justify-between', 'text-sm', styles.container]) }}>
        {# frontend_module(3) - ToDo: Use Slots #}
        <div{{ attrs().addClass('flex', 'items-center', 'gap-x-4', 'align-center') }}>
            <div{{ attrs().addClass('hidden', 'min-[480px]:block') }}>
                {# frontend_module(4) - ToDo: Use Slots#}
            </div>
        </div>
    </div>
</div>
